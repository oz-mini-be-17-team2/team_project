[tool.poetry]
name = "team-project"
version = "0.1.0"
description = ""
authors = ["gunung-kim"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.14"
pandas = "^3.0.1"
fastapi = "^0.135.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
